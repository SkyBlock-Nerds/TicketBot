name: Java CI

on:
  pull_request:
    branches:
      - '*'
    paths:
      - '**.java'
      - 'pom.xml'
  push:
    branches:
      - '*'
    paths:
      - '**.java'
      - 'pom.xml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Check EditorConfig
        run: mvn -B -ntp org.ec4j.maven:editorconfig-maven-plugin:check

      - name: Compile
        run: mvn -B -ntp clean compile

      - name: Run tests
        run: mvn -B -ntp test

      - name: Package
        run: mvn -B -ntp package -DskipTests
